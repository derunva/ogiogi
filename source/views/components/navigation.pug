include ./settings.pug
.header
  .wrapper
    .row.row-header
      .logo Company Name
      .navbar
        .burger
          span
          span
          span
        nav.menu
          ul
            each element in mainMenu
              li
                a(href=element.link)=element.title
